{
  "name": "reaml-ppx",
  "version": "0.1.0",
  "license": "MIT",
  "files": [
    "bsconfig.json",
    "src",
    "tests"
  ],
  "scripts": {
    "start": "bsb -make-world -backend native -w",
    "build": "bsb -make-world -backend native",
    "clean": "bsb -clean-world",
    "postinstall": "bsb -make-world -backend native && cp lib/bs/native/reaml_ppx.native ppx"
  },
  "dependencies": {
    "bsb-native": "^4.0.7000"
  }
}
